name: First workflow
on:
    push:
        branches:
            - dev
            - main
    pull_request: 
        branches:
            - dev
    workflow_dispatch:
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo "Hello World"
                ls 
              name: Starting
    checkout:
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout the repo
              uses: actions/checkout@v3
            - run: ls
          
              
              
